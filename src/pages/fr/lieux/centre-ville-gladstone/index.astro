---
import LocationPage from '../../../../components/LocationPage.astro';
import { locations } from '../../../../data/site';

const location = locations.find((candidate) => candidate.id === 'gladstone')!;
---

<LocationPage locale="fr" location={location} />
